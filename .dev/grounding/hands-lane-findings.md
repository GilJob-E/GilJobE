# Hands 레인 + 속삭임 변별 — 기준 파일 실측 기록 (2026-06-12)

기준 파일(giljob-docker `qa/`, gitignored): `.audio_test.mp4`(속삭임+느린 발화 14s),
`.vision_test.mp4`(손가락 3→2→5, 25s). 재현: `mmm_bench_baseline.py`(레인 종단),
`hands_probe.py`(프레임 단위 진단). 결과 JSON: `mmm_bench_baseline.json`.

## 결론 요약

| 기준 | 강화 전 | 강화 후 |
|---|---|---|
| 속삭임 | "유성 구간 부족" + 조음 0.0음절/초(발화 없음처럼 보임) | 유성 비율 0%(무성 우세=속삭임형) + 강도 피크 2.59/s 참고치 |
| 손가락 3→2→5 | 레인 없음(손목 가시성만) | **4→2→5** — 2·5 정확, '3'은 MediaPipe 한계로 4(아래) |

## 손가락 카운트 기준 선정 (기각 이력 포함)

1. **PIP 관절각(2D 정규화)** — 기각. 카메라 쪽 접힘이 단축돼 일직선으로 보임('3'의 접힌
   새끼 166~176°). 
2. **PIP 관절각(3D world)** — 기각. 가려진 손가락은 랜드마크 자체가 '펴짐' 기하로 환각됨.
3. **채택: 손목 반경비**(tipR/pipR, FINGER_RADIAL=1.25) — 펴짐 1.4대 / 접힘 0.6~0.7 /
   환각 경계 1.1. **엄지: 끝→중지MCP 거리/손바닥길이**(THUMB_AWAY=0.75) — 접힘 0.55~0.67
   / 폄 0.83+. 관절각은 접힌 엄지도 176°라 무용(주먹 위를 감싸는 해부학).
4. 스무딩: 중위수(±2행) + 0.4s 안정 구간. p25(환각이 위쪽 편향이므로)도 실험 — VIDEO
   모드에선 차이 없음(환각이 플리커가 아니라 지속이라).

## '3'→4 오검출은 모델 한계 (소진한 옵션)

'3' 자세 = 중지+약지+새끼 폄, **검지+엄지가 손바닥 뒤로 접혀 카메라에서 완전 가림**.
MediaPipe가 가려진 검지를 모든 경로에서 "그럴듯하게 펴진" 랜드마크로 환각:

- VIDEO/IMAGE 모드, 960·1280·1920 해상도, min_tracking/presence 0.5~0.9 전부 동일 경향
- 체인길이(IMAGE 모드에선 0.44로 뭉개져 변별 가능했으나) VIDEO 모드는 0.93으로 실손가락
  (0.91)과 동일 — 추적이 완결된 가짜 손가락을 만듦
- presence 0.9는 검출 자체가 죽고(커버리지 전멸), IMAGE 모드는 '2' 구간까지 오염 → VIDEO 유지

즉 기하 후처리로 복구 불가. 가림 없는 일반 셈 제스처(2·5·주먹)는 정확. 필요해지면
후속 후보: 손가락별 신뢰 게이팅(모델 교체/앙상블) 또는 "검지·엄지 가림형 3" 패턴 인식.

## QIVD 95문항 임계 스윕 (2026-06-12 후속)

`.vision_test` 1개로 정한 임계(radial 1.25 / thumb 0.75)를 QIVD `object_counting`의
"How many fingers…" **95문항**(정수 답 0~10, 양손 포함, 정답 라벨 존재)으로 재검증.
랜드마크 1회 추출 캐시(`qivd_finger_landmarks.json`) 후 임계 그리드 스윕
(`qivd_finger_sweep.py`).

- 1.25/0.75 (구): 전체 0.684 · 레인답변가능 0.714. 오차가 **과소 카운트 지배**(과소 20 vs 과대 6)
  — 측면/약간 굽은 손가락을 접힘으로 오판.
- **1.15/0.68 (신, 스윕 최적)**: 전체 0.737 · 레인답변가능 0.769. 과소 20→9로 개선
  (과대 6→12 trade-off, 순증). radial 1.15는 환각 검지(1.1) 위 마진 유지 → 단위테스트
  `test_count_rejects_hallucinated_marginal_finger` 통과.

레인 범위 밖(adapter/VLM 영역, 91문항 기준에서 제외):
- "How many fingers on my **right hand**?" (2건) — 우리는 양손 합산 → 어느 손인지 질문 파싱 필요
- "How many fingers am I **hiding**?" — 숨긴 손가락(보이지 않는 것) 셈, 기하로 불가
- "**combine both hands**" — 양손 합산은 맞지만 표현 다양

미검출 4건(IMAGE 모드 + 검출 신뢰도 0.5): 손 자체가 안 잡힘 — 검출 신뢰도/윈도우 폭 후속 검토.

## 속삭임 변별 설계

- `voiced_ratio`를 피치 실패 시에도 운반(_pitch_metrics 조기 반환 + _agg_pitch not-valid 분기)
- `intensity_peak_nuclei`(유성성 게이트 전 피크) + `phonation_s`(집계 합산용) 신설 —
  유성핵 0이어도 "강도 피크 N/s 참고치"로 속도가 남는다(속삭임에서 조음 0.0 오독 방지)
- describe/fragment: "무성 우세 발성 = 속삭임형 신호" 명시(해석 라벨 아닌 음향 사실)
