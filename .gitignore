# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# venv
.venv/
venv/

# build artifacts (수정 금지 산출물)
build/
dist/
out/

# secrets / env (HF_TOKEN 등 — 커밋 금지)
.env
.env.*

# local Claude settings (team-shared rules는 커밋, local은 제외)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# 테스트 미디어 픽스처(바이너리 — 커밋 안 함; 스파이크/라이브 e2e가 로컬에서 참조).
# 재현엔 한국어 오디오 클립이 repo 루트에 필요(handoff 참고).
*.mp4
*.wav
*.m4a
*.webm

# parked facial-detection 탐색 스크래치(메모 vision-nonverbal-instability — 우선순위 아님).
# venv·모델 가중치라 무거움 → 실수 커밋 방지.
/.dev/.venv-vision/
/.dev/models/
